import { RespectNewLinesPipe } from './respect-new-lines.pipe';

describe('RespectNewLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new RespectNewLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
